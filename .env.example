# Electron Forge Build Configuration
# Copy this file to .env and fill in your actual values for production builds
# Command: cp .env.example .env  (or Copy-Item .env.example .env on Windows)

# =============================================================================
# macOS App Store Code Signing (mas build)
# =============================================================================
# Get these from your Apple Developer account at https://developer.apple.com
# Certificate names must match exactly what's in your Keychain Access

# Apple Distribution certificate (for app signing)
CSC_NAME=""

# Apple Installer certificate (for PKG signing)
CSC_INSTALLER_NAME=""

# =============================================================================
# Windows Store Configuration (MSIX build)
# =============================================================================
# Get these from Microsoft Partner Center > Product Identity
# https://partner.microsoft.com/dashboard

# Publisher ID (CN=...) - Found in Partner Center > Product Identity
WIN_PUBLISHER_ID=""

# Publisher Display Name - Your company/developer name
WIN_PUBLISHER_NAME=""

# Package Name - Reserved name in Partner Center (e.g., CompanyName.AppName)
WIN_PACKAGE_NAME=""

# Package Display Name - Human-readable app name
WIN_PACKAGE_DISPLAY_NAME=""

# Identity Name - Reserved in Partner Center (usually same as Package Name)
WIN_IDENTITY_NAME=""

# Windows SDK Path (optional - only if not at default location)
# WINDOWS_KIT_PATH="C:\\Program Files (x86)\\Windows Kits\\10\\bin\\10.0.22621.0\\x64"

# =============================================================================
# Notes
# =============================================================================
# - Leave environment variables empty to use default values from forge.config.js
# - For production builds, fill in your actual values here or set in your CI/CD
# - The .env file is git-ignored and will NOT be loaded automatically
# - To use .env file, install dotenv-cli: npm install -g dotenv-cli
#   Then run: dotenv npm run make
